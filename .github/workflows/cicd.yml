name : Cantina
on : [push, pull_request]
jobs:
  cantina-compile:
    runs-on: ubuntu-latest
    steps:
      - name: Cantina Compilation
      - uses: actions/checkout@v2
      - run: sudo apt --assume-yes install maven
      - run: mvn compile

  cantina-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Installation Maven
      - uses: actions/checkout@v2
      - run: sudo apt --assume-yes install maven

      - name: Cantina Tests Unitaires
      - uses: actions/checkout@v2
      - run: mvn test > javatest.txt

      - name: Cantina Tests Intégration
      - uses: actions/checkout@v2
      - run: mvn verify > javatest.txt

      - name: Upload Tests Resultats
        uses: actions/upload-artifact@v2
        with:
          name: Tests Resultats
          path: javatest.txt
